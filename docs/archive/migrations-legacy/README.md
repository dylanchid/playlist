# Legacy SQL migrations (archived)

These files were used for **one-off** Supabase Dashboard migrations before the repo adopted versioned CLI migrations.

**Do not run on new environments.** Use [`supabase/migrations/`](../../../supabase/migrations/) and `npm run db:push` instead.

See [NEXT_STEPS.md](../../../NEXT_STEPS.md) and [OPERATIONS_AND_SECURITY.md](../../OPERATIONS_AND_SECURITY.md).
